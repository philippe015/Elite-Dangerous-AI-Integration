# --- Cœur du système (IA & Audio) ---
openai>=1.0.0
# Version plus souple de numpy pour éviter les conflits Linux/Windows
numpy>=1.24.0
speechrecognition>=3.10.0
# Utiliser 'sounddevice' est souvent plus stable que PyAudio sur Linux moderne
sounddevice>=0.4.6
scipy>=1.10.0

# --- Réseau & Outils ---
requests>=2.31.0
httpx>=0.24.0
python-socketio>=5.10.0
python-engineio>=4.8.0
aiohttp>=3.9.0

# --- Interface (Si utilisée) ---
flask>=3.0.0
flask-socketio>=5.3.0

# --- DÉPENDANCES CONDITIONNELLES (Le secret de la compatibilité) ---
# Ces lignes ne s'installeront QUE si l'utilisateur est sur Windows.
# Cela empêche le build Linux de planter.
pywin32>=306; sys_platform == 'win32'
winspeech>=1.0.1; sys_platform == 'win32'
pyttsx3>=2.90; sys_platform == 'win32'

# --- Dépendances Linux (Optionnel mais recommandé pour le TTS local) ---
# espeakng; sys_platform == 'linux'

# --- Autres utilitaires ---
colorama>=0.4.6
pillow>=10.0.0
python-dotenv>=1.0.0
